---
name: RenderFields
route: /RenderFields
menu: Components
---

import { Playground, Props } from 'docz'
import RenderFields from './RenderFields'

# RenderFields

Render fields in view mode or edit mode. Commonly used for editing a content(See source code of actions/Edit.tsx).

## Properties
<Props table={true} isToggle={true} of={RenderFields} />
